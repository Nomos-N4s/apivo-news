# Copy to .env for local development. Never commit real credentials.
DATABASE_URL=postgres://apivo:apivo@localhost:5432/apivo?sslmode=disable
HTTP_ADDR=:8080
APP_ENV=dev
LOG_LEVEL=info
# JWT verification for the editorial endpoints. Leave JWKS_URL unset to run
# without them (they are not mounted); JWT_AUDIENCE is optional and only
# meaningful alongside JWKS_URL.
#JWKS_URL=https://<project>.supabase.co/auth/v1/.well-known/jwks.json
#JWT_AUDIENCE=authenticated

# web/ (Astro server): where the reader pages find the Go API. Leave unset
# to preview the reader screens from built-in fixtures (until T023/T024).
#API_BASE_URL=http://localhost:8080

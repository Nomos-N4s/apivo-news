# Dependency and build output — rebuilt inside the image.
node_modules
dist
.astro

# Local secrets and environment files must never enter the build context.
.env
.env.*
*.pem
*.key

# Repo noise irrelevant to the image.
.git
coverage
Dockerfile
.dockerignore
